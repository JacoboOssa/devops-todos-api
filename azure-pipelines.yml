# azure-pipelines.yml for todos-api

trigger:
  branches:
    include:
    - main # Or your default branch
  
resources:
- repo: self

variables:
  # Agent VM image name
  vmImageName: 'ubuntu-latest'

  # Azure Container Registry connection details
  # IMPORTANT: Replace 'YourACRServiceConnectionName' with the actual name of your ACR Service Connection in Azure DevOps
  dockerRegistryServiceConnection: 'YourACRServiceConnectionName' 
  acrLoginServer: 'ingesoft5-bbfcc8fmg7bvghf9.azurecr.io' # From the image
  imageRepository: 'todos-api' # Name for the image in ACR
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
  buildContextPath: '$(Build.SourcesDirectory)'
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build and Push stage
  jobs:
  - job: Build
    displayName: Build and Push Todos API image
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and Push $(imageRepository)
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        buildContext: $(buildContextPath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
          latest
